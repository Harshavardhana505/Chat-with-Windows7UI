<script>
//A Javascript Program to illustrate Caesar Cipher Technique
	
	// Encrypts text using a shift od s
	function encrypt(text, s)
	{
		let result=""
		for (let i = 0; i < text.length; i++)
		{
			let char = text[i];
			if (char.toUpperCase(text[i]))
			{
				let ch = String.fromCharCode((char.charCodeAt(0) + s-65) % 26 + 65);
				result += ch;
			}
			else
			{
				let ch = String.fromCharCode((char.charCodeAt(0) + s-97) % 26 + 97);
				result += ch;
			}
		}
		return result;
	}
	function wapo(){
	setTimeout(function(){ pout() }, 3000);
	}
	
	function pout(){
	// Driver code
	let text = document.getElementById('text').value;
	let s = document.getElementById('s').value;
	document.write("Text : " + text + "<br>");
	document.write("Shift : " + s + "<br>");
	document.write("Cipher: " + encrypt(text, s) + "<br>");
	}
	// This code is contributed by avanitrachhadiya2155
</script>

Message: <input type='text' id='text'/>
Shift:   <input type='number' id='s' oninput='wapo()'>
